# Frontend (Vite) environment variables
#
# IMPORTANT: Anything prefixed with VITE_ is bundled into the browser.
# Do NOT put API keys/secrets here.

# Optional: base URL for the deployed backend (serverless API)
# If omitted, the frontend calls /api/fetch-user-data on the same origin.
VITE_API_BASE_URL=
